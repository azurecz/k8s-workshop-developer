apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: myappspa
  labels:
    app: myappspa
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: "/"
spec:
  rules:
    - host: 0.0.0.0.xip.io
      http:
        paths:
          - path: /
            backend:
              serviceName: myappspa
              servicePort: 8080
