apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: myappspa
  labels:
    app: myappspa
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: "/"
spec:
  rules:
    - host: 40.113.68.126.xip.io
      http:
        paths:
          - path: /
            backend:
              serviceName: myappspa
              servicePort: 80
